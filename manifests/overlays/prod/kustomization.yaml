apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../base
- pvc.yaml
configMapGenerator:
- files:
  - application.yml
  name: "sampleapp1-82240947-configmap"
patches:  # patchesStrategicMerge에서 patches로 변경
- deployment.yaml
- ingress.yaml
